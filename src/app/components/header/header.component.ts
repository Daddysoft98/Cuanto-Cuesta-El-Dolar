import { Component } from '@angular/core';

@Component({
  selector: 'app-header', // Asegúrate de que este selector coincida
  templateUrl: './header.component.html', // Ruta al archivo HTML
  styleUrls: ['./header.component.css'], // Ruta al archivo CSS
  standalone: true
})
export class HeaderComponent {}
